{
  "name": "realm-edge-gateway",
  "version": "1.0.0",
  "description": "Bridges weighbridge indicators (serial/TCP) to the REALM Group Freight ingest API.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "node test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "serialport": "^12.0.0",
    "@serialport/parser-readline": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
